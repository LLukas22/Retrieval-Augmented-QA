name: Build & Push ST4-Container

on:   
  workflow_dispatch:

  release:
    types: [published]

jobs:

  build:

    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
    
      - name: Log in to the Container registry
        uses: docker/login-action@f054a8b539a109f9f41c372932f1ae047eff08c9
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 'Build and push Docker image'
        env:
          DOCKER_BUILDKIT: 1
        run: |
          docker build ./src/importer --file ./src/importer/ST4.Dockerfile --tag ghcr.io/llukas22/retrieval-qa-st4:latest
          docker push ghcr.io/llukas22/retrieval-qa-st4:latest